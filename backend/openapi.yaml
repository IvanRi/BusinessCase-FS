openapi: 3.0.3
info:
  title: API de ventas
  version: 0.1.0
  description: Alta, carga CSV, consolidado y detalle paginado de ventas.
paths:
  /health:
    $ref: ./openapi/paths/health.yaml
  /ventas:
    $ref: ./openapi/paths/ventas.yaml
  /ventas/csv:
    $ref: ./openapi/paths/ventas-csv.yaml
  /ventas/consolidado:
    $ref: ./openapi/paths/ventas-consolidado.yaml
components:
  schemas:
    $ref: ./openapi/components/schemas.yaml
